{
  "name": "ai-audit-gate-backend",
  "version": "1.0.0",
  "description": "Backend API for AI Audit Gate — NAB Vietnam internship project",
  "main": "src/server.js",
  "scripts": {
    "postinstall": "prisma generate",
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "npx prisma migrate dev --name init",
    "studio": "npx prisma studio",
    "seed": "node prisma/seed.js",
    "reset": "npx prisma migrate reset --force && node prisma/seed.js",
    "setup": "npx prisma migrate dev --name init && node prisma/seed.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "pdf-parse": "^1.1.1",
    "prisma": "^5.22.0"
  }
}
